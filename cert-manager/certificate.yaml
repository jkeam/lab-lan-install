apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: app-tls-cert
spec:
  isCA: false
  secretName: app-tls-cert-secret
  issuerRef:
    name: letsencrypt-digitalocean
    kind: ClusterIssuer
  dnsNames:
  - keam.org
  - '*.keam.org'
